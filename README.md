# algorithm
Learning algorithms with Ruby

## usage
irb
require './require_all.rb'

HashMap.new
h = HashMap.new
h.put("key", "value")
h.put("a", 1)
h.get("key")

## test
`bundle install`

`bundle exec rspec`
